# Spark-EP
only example for return true result

## Maven:
```Markdown
<dependencies>
    <!-- Spark Java -->
    <dependency>
        <groupId>com.sparkjava</groupId>
        <artifactId>spark-core</artifactId>
        <version>2.9.3</version> <!-- Güncel sürümü kontrol edin -->
    </dependency>

    <!-- Bean Validation -->
    <dependency>
        <groupId>javax.validation</groupId>
        <artifactId>validation-api</artifactId>
        <version>2.0.1.Final</version> <!-- Güncel sürümü kontrol edin -->
    </dependency>
</dependencies>
```
